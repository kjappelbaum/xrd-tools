name: Docker Image Build CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - name: Build the Docker image
        run: pip3 install requests[security] && ./build_docker.sh && ./run_docker.sh && cd test && python3 test_docker.py
