name: Docker Image Build CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - name: Build the Docker image
        run: pip install requests && ./build_docker.sh && ./run_docker.sh && cd test && python test_docker.py
